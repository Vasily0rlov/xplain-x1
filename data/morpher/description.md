This folder contains datasets representing a small subset of data (with key feature) for Russian word declension.

morpher-sample-single-words-only.csv - dataset with rows representing individual words

morpher-sample-sentences.csv - dataset with multiple rows representing sentences (in the order of appearance), separated by rows with "." as the key entry.
